services:
    pgvector:
        container_name: pgvector
        image: pgvector/pgvector:pg${DB_VERSION:-18}-trixie
        restart: unless-stopped
        # user: "${UID:-1000}:${GID:-1000}"
        environment:
            TZ: "${TIMEZONE:-Europe/Warsaw}"
            POSTGRES_USER: "${DB_USER:-root}"
            POSTGRES_PASSWORD: "${DB_PASSWORD:-secret}"
            POSTGRES_DB: "${DB_NAME:-default}"
        ports:
            - ${DB_PORT:-5432}:5432
        networks:
            shared-network-internal:
        volumes:
            - shared-home:/home/${APP_USER:-app}
            - pgvector:/var/lib/postgresql/${DB_VERSION:-18}/docker
            - ./docker/pgvector/bin/backup.sh:/bin/backup.sh:ro
            - ${BACKUP_DIR:-${HOME}/data/backups/docker/pgvector}:/backup
        labels:
            ofelia.enabled: "true"
            ofelia.job-exec.pgvector.command: "/bin/backup.sh"
            ofelia.job-exec.pgvector.schedule: "${BACKUP_CRON_EXPRESSION:-0 9,15,17,21 * * *}"

networks:
    shared-network-internal:
        name: ${SHARED_NETWORK_INTERNAL_NAME:-shared-network-internal}
        external: true

volumes:
    shared-home:
        name: ${SHARED_HOME_VOLUME:-shared-home}
        external: true
    pgvector:
        name: pgvector
